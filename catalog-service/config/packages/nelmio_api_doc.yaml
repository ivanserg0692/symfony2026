nelmio_api_doc:
    documentation:
        info:
            title: Catalog Service API
            description: Catalog service OpenAPI specification.
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc(?:\.json)?$) # Accepts routes under /api except /api/doc and /api/doc.json
